// Stack-allocated variables test
public int testVars(int param) {
    int zero; // Variables with no expression are initialized to zero
    int another = -10;

    // Local variable used in expression initializing another local variable
    int paramSqr = param * param + another;

    another = zero - (param + paramSqr);
    return another;
}
